# Productos e imágenes — Beauty Boost

La carga de productos e imágenes fue integrada en un único archivo:

`BEAUTY_BOOST_BASE_DATOS_UNIFICADA.sql`

No ejecutes un segundo SQL de productos.

Los archivos de imagen se encuentran en `web/img/` y las rutas se guardan en `producto.imagen_url`.
